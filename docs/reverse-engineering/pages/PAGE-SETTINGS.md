# PAGE-SETTINGS：设置页

## 身份信息

| 字段 | 值 |
| --- | --- |
| 页面 ID | PAGE-SETTINGS |
| 页面名称 | 设置 |
| 旧版入口路径 | 左侧导航「设置」 |
| 巡检优先级 | P0 |

## 证据

| 字段 | 值 |
| --- | --- |
| 证据 ID | EV-RUNTIME-010 |
| 最高证据等级 | E1 |
| 可信度 | high |
| 关联资产 | `docs/截图/设置.png` |

## 页面区域

| 区域 | 可见内容 | 备注 |
| --- | --- | --- |
| 左侧导航 | 首页、声音管理、形象管理、素材管理、任务中心、账号管理、设置、帮助 | 全局导航入口；当前选中「设置」 |
| 左侧设置子导航 | 常规设置、模型设置、提示词管理、数据设置、软件更新 | 设置页内二级导航；当前选中「常规设置」 |
| 顶部区域 | 应用标题「轻语 IP」、汉堡菜单、用户下拉「chur」带 VIP 图标、错误日志上传按钮 | 用户状态与全局支持入口 |
| 主工作区 | 常规设置表单：界面主题、统一输出根目录、音频 / 视频 / 封面输出路径 | 仅「常规设置」标签内容可见 |

## 可见控件

| 控件 | 类型 | 启用状态 | 观察到的行为 |
| --- | --- | --- | --- |
| 常规设置 标签 | tab | 选中 | 展示文件存储路径和界面主题配置 |
| 模型设置 标签 | tab | 未选中 | 入口可见，内容未在截图中展示 |
| 提示词管理 标签 | tab | 未选中 | 入口可见，内容未在截图中展示 |
| 数据设置 标签 | tab | 未选中 | 入口可见，内容未在截图中展示 |
| 软件更新 标签 | tab | 未选中 | 入口可见，内容未在截图中展示 |
| 界面主题 下拉框 | select | 启用 | 当前值为「默认主题」，切换后立即生效 |
| 统一输出根目录 输入框 + 选择按钮 | text + button | 启用 | 显示 `/Users/yuzhenzhao/Documents/outputs`，点击「选择」应打开目录选择器 |
| 音频输出路径 输入框 | text | 启用（只读/自动派生） | 显示 `/Users/yuzhenzhao/Documents/outputs/audios` |
| 视频输出路径 输入框 | text | 启用（只读/自动派生） | 显示 `/Users/yuzhenzhao/Documents/outputs/videos` |
| 封面输出路径 输入框 | text | 启用（只读/自动派生） | 显示 `/Users/yuzhenzhao/Documents/outputs/thumbs` |
| 出现错误？点击此处上传日志 按钮 | button | 启用 | 点击后应上传日志或跳转到支持页面 |
| 用户下拉「chur」 | dropdown | 启用 | 显示用户名和 VIP 图标，可能包含账号/授权相关入口 |

## 表单字段

| 字段 | 输入类型 | 默认值或示例值 | 校验或帮助文案 |
| --- | --- | --- | --- |
| 界面主题 | select | 默认主题 | 选择应用的配色主题，更换后立即生效 |
| 统一输出根目录 | text + 目录选择按钮 | `/Users/yuzhenzhao/Documents/outputs` | 只需选择一个根目录，系统将自动在该目录下生成 audios / videos / drafts / exports / thumbs 子目录 |
| 音频输出路径 | text（自动派生） | `/Users/yuzhenzhao/Documents/outputs/audios` | 由根目录自动派生：audios |
| 视频输出路径 | text（自动派生） | `/Users/yuzhenzhao/Documents/outputs/videos` | 由根目录自动派生：videos |
| 封面输出路径 | text（自动派生） | `/Users/yuzhenzhao/Documents/outputs/thumbs` | 由根目录自动派生：thumbs（素材视频封面存放目录） |

## 弹窗与提示

| 触发方式 | 弹窗或提示 | 操作按钮 | 结果 |
| --- | --- | --- | --- |
| 点击「出现错误？点击此处上传日志」 | 未知，截图未展示弹窗内容 | 未知 | 可能打开日志上传或支持反馈通道 |

## 状态变化

| 操作 | 操作前 | 操作后 | 证据 ID |
| --- | --- | --- | --- |
| 切换界面主题 | 当前主题 | 应用新主题并立即生效 | EV-RUNTIME-010 |
| 选择统一输出根目录 | 旧根目录 | 新根目录，且音频 / 视频 / 封面输出路径自动更新为子目录 | EV-RUNTIME-010 |

## 可执行动作

| 动作 | 结果 | 关联功能卡 |
| --- | --- | --- |
| 切换界面主题 | 改变应用配色主题 | FC-PROVIDER-SETTINGS |
| 选择统一输出根目录 | 更新所有派生输出路径 | FC-PROVIDER-SETTINGS |
| 切换设置子标签 | 进入模型设置、提示词管理、数据设置或软件更新 | FC-PROVIDER-SETTINGS |
| 上传日志 | 将本地日志发送给支持方或导出 | FC-PROVIDER-SETTINGS |

## 受限动作

| 动作 | 障碍 | 运行障碍 ID |
| --- | --- | --- |
| 查看模型设置、提示词管理、数据设置、软件更新的具体内容 | 截图仅展示「常规设置」标签，其余标签内容未可见；是否需要登录 / 激活 / 云服务才能保存或校验尚不明确 | 待 Task 3 后续补证 |

## 关联功能卡

- [FC-PROVIDER-SETTINGS](../function-cards/FC-PROVIDER-SETTINGS.md)

## 待确认问题

- 「模型设置」标签内是否包含 API Key、Base URL、模型选择、服务状态校验和连通测试？
- 「提示词管理」标签是管理改写提示词模板，还是包含系统提示词 / 角色设定？
- 「数据设置」标签是否涉及缓存清理、数据导出 / 导入、本地数据库位置？
- 「软件更新」标签是否显示当前版本、检查更新按钮、自动更新开关？
- 设置保存是自动保存还是需点击保存按钮？截图未展示保存按钮。
- 用户下拉「chur」中的 VIP 图标是否影响设置页可用功能？
