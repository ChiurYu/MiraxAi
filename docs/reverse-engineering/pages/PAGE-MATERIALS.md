# PAGE-MATERIALS：素材管理

## 身份信息

| 字段 | 值 |
| --- | --- |
| 页面 ID | PAGE-MATERIALS |
| 页面名称 | 素材管理 |
| 旧版入口路径 | 左侧导航「素材管理」 |
| 巡检优先级 | P1 |

## 证据

| 字段 | 值 |
| --- | --- |
| 证据 ID | EV-RUNTIME-120 |
| 最高证据等级 | E1 |
| 可信度 | high |
| 关联资产 | `docs/截图/素材管理.png` |

## 页面区域

| 区域 | 可见内容 | 备注 |
| --- | --- | --- |
| 左侧导航 | 首页、声音管理、形象管理、素材管理（选中）、任务中心、账号管理、设置、帮助 | 全局导航入口 |
| 顶部区域 | 应用标题「轻语 IP」、汉堡菜单、用户下拉「chur」带 VIP 图标、窗口控制按钮 | 全局顶栏 |
| 主工作区 | 顶部工具栏；左侧「分类管理」边栏；右侧素材列表/空状态 | 分类、上传、搜索、管理在同一页完成 |

## 可见控件

| 控件 | 类型 | 启用状态 | 观察到的行为 |
| --- | --- | --- | --- |
| 页面标题 | heading | 启用 | 「素材管理」，副标题「管理视频素材的分类、上传、标记和向量化」 |
| 帮助 按钮 | button | 启用 | 位于标题右侧，截图未展示帮助内容 |
| 新建分类 按钮 | button | 启用 | 左侧工具栏，点击后可能弹出分类名称输入框 |
| 上传视频或图片 按钮 | button | 启用 | 左侧工具栏，点击后应打开文件选择器 |
| 搜索素材 输入框 | search | 启用 | placeholder「搜索素材...」，右侧带搜索按钮 |
| 分类管理 标题区 | section header | 启用 | 显示「分类管理」和刷新按钮 |
| 全部素材 分类项 | list item / tab | 选中 | 显示分类名称「全部素材」和数量徽章「0」 |
| 全选 复选框 | checkbox | 禁用（空状态） | 列表为空时无素材可勾选 |
| 列表标题 | heading | 启用 | 「全部素材（0）」，右侧有刷新按钮 |
| 空状态提示 | empty state | 启用 | 视频图标 + 「暂无素材」 + 「上传第一个视频」按钮 |

## 表单字段

| 字段 | 输入类型 | 默认值或示例值 | 校验或帮助文案 |
| --- | --- | --- | --- |
| 搜索关键词 | search | 空 | placeholder「搜索素材...」 |
| 新建分类名称 | text（推断） | 空 | 截图未展示新建分类弹窗，输入类型待确认 |
| 上传文件 | file | 空 | 支持视频或图片，具体格式限制未在截图中展示 |

## 弹窗与提示

| 触发方式 | 弹窗或提示 | 操作按钮 | 结果 |
| --- | --- | --- | --- |
| 点击「新建分类」 | 截图未展示弹窗 | 未知 | 可能要求输入分类名称 |
| 点击「上传视频或图片」/「上传第一个视频」 | 截图未展示文件选择器 | 未知 | 可能打开系统文件选择器并支持批量选择 |
| 上传完成后 | 截图未展示提示 | 未知 | 可能展示上传进度、抽帧/向量化状态 |

## 状态变化

| 操作 | 操作前 | 操作后 | 证据 ID |
| --- | --- | --- | --- |
| 点击新建分类 | 分类列表仅「全部素材」 | 新增自定义分类 | EV-RUNTIME-120（推断） |
| 点击上传 | 空状态 | 展示上传进度，完成后素材列表出现卡片 | EV-RUNTIME-120（推断） |
| 切换分类 | 全部素材（0） | 显示该分类下的素材 | EV-RUNTIME-120（推断） |
| 输入搜索关键词 | 全部素材 | 过滤后的素材结果 | EV-RUNTIME-120（推断） |

## 可执行动作

| 动作 | 结果 | 关联功能卡 |
| --- | --- | --- |
| 新建分类 | 创建自定义素材分类 | FC-ASSET-MANAGEMENT |
| 上传视频或图片 | 将素材加入素材库 | FC-ASSET-MANAGEMENT |
| 搜索素材 | 按关键词过滤素材 | FC-ASSET-MANAGEMENT |
| 刷新分类/列表 | 重新加载分类和素材 | FC-ASSET-MANAGEMENT |
| 全选/批量操作 | 对素材进行批量管理（空状态时不可见） | FC-ASSET-MANAGEMENT |
| 编辑/删除分类或素材 | 维护素材库结构（具体入口未在截图中展示） | FC-ASSET-MANAGEMENT |

## 受限动作

| 动作 | 障碍 | 运行障碍 ID |
| --- | --- | --- |
| 上传真实素材文件 | 需要本地文件系统和 FFmpeg 支持；截图未展示上传后的处理流程 | RB-ASSET-003 |
| 素材自动抽帧、标记和向量化 | 需要本地 Python 服务或云端向量模型；当前素材库为空，未验证 | RB-ASSET-003 |
| 搜索/标签/批量操作行为 | 截图展示空状态，真实交互未验证 | RB-ASSET-003 |

## 关联功能卡

- [FC-ASSET-MANAGEMENT](../function-cards/FC-ASSET-MANAGEMENT.md)

## 待确认问题

- 素材上传是否支持文件夹拖拽、批量上传、断点续传？
- 素材支持的文件格式、大小、分辨率限制是什么？
- 素材「向量化」具体指什么？是生成视频embedding用于搜索，还是生成抽帧/封面？
- 每个素材是否必须关联分类？是否支持标签、描述、封面图？
- 素材库与首页「学习对标」阶段的视频导入是否为同一数据源？
- 删除分类时是否会级联删除素材或弹出确认？
