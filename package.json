{
  "name": "mirax-ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "vitest run --passWithNoTests",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
